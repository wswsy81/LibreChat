B3 UX-002 / DATA-006 / DATA-007 / DATA-008 red reproduction
Date: 2026-07-18 Asia/Shanghai

Client behavioral regression suite before fixes:
- 0/4 passed.
- Two-character name: after moving health slider, enter-studio button remained disabled.
- One-slider onboarding: expected {health:4}, received {health:4,work:5,play:5,love:5}.
- Clearing occupation/city: basics mutation was never called.
- 2021-02-31 solar date: birth mutation was called instead of being rejected.

API route before fixes:
- 9/10 passed.
- POST /api/life/onboarding with {dashboards:{health:5}} returned 422 before idempotency handling.

Production cross-layer reproduction after the first B3 deploy:
- Normal /about UI sent {occupation:null,city:null} and showed "已存进你的档案".
- The persisted profile contained occupation:"null" and city:"null" instead of removing the fields.
- Root cause: api/server/routes/life.js applied String(value) to every provided field, converting JSON null into the literal text "null" before forwarding to future-engine.
- The new API-boundary regression failed 10/11 and captured the exact forwarded body {occupation:"null",city:"null"}.

These failures reproduce the audit findings without production writes.
